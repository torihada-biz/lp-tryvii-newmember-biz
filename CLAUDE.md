# CLAUDE.md — lp-tryvii-newmember-biz

TryVII 新メンバー募集LP の本番リポジトリ。

## 概要
- GitHub: `torihada-biz/lp-tryvii-newmember-biz`
- デプロイ: Cloudflare Pages

## 構成（重要）
ルート直下に `index.html` / `styles.css` / `script.js` / `asset/`（単数形）を置く。
**CF Pages がルートを配信するため `src/` 化しない**こと。

## デザイン・素材
- デザイン正本は **Figma（node 9:90）**
- 素材は `asset/`（単数）。`head01`〜`head07` = 見出し画像
- 制作元データ（AI / ラフ案）は `~/Documents/20260601_TryVII新メンバー募集LP/`
